{
  "name": "convergence-health-planner",
  "version": "1.0.0",
  "private": true,
  "description": "A small web app for organizing symptom overlap and diet discussion notes across multiple conditions.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
